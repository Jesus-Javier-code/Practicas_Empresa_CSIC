---
title: "Prácticas CSIC"
format: 
  dashboard:
    orientation: columns
    nav-buttons: [github]
    github: https://github.com/Jesus-Javier-code/Practicas_Empresa_CSIC.git
    theme:
      - flatly
logo: images/Logotipo_del_CSIC.png
logo-alt: "CSIC logo."
---

```{python}
#| label: load-packages
from plotnine import *
import great_tables as gt
import pandas as pd
import glob
from IPython.display import Image
import os
```
# 🏝️ LA PALMA
```{python}
#| label: palma-image
#| fig-cap: "Análisis de Potencia Radiativa - La Palma"
display(Image(filename="images/graficamala.png", width=800))
```

```{python}
'''
#| label: load-palma-data
# Cargar todos los archivos CSV de la carpeta
folder_path = "../data/processed/brightness_temperature_by_Year_Month"
csv_files = glob.glob(os.path.join(folder_path, "brightness_temperature_radiance_*.csv"))

# Leer y combinar todos los archivos
all_data = pd.concat([pd.read_csv(f) for f in csv_files])

# Verificar nombres reales de columnas (para debug)
print("Columnas disponibles:", all_data.columns.tolist())

# Mostrar tabla con great_tables (versión corregida)
(
    gt.GT(all_data.head(20))
    .tab_header(title="Datos de Radiancia y Brillo")
    .fmt_number(
        columns=all_data.select_dtypes(include=['float64']).columns.tolist(), 
        decimals=2
    )
    .opt_interactive()  # Versión correcta del método
)
'''
```

# 🌋 EL TEIDE
```{python}
#| label: teide-image
#| fig-cap: "Análisis de Potencia Radiativa - El Teide"
display(Image(filename="images/graficamala.png", width=800))  # Asegúrate de tener esta imagen
```

